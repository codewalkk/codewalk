//! `codewalk` — mode 2 CLI. codegraph + learned intelligence.
//!
//! Lands at M5. Adds `codewalk_ask`/`learn`/`status` MCP tools, `update` (mine
//! transcripts), and `install` (wire into Claude Code at LOCAL scope by default).
//! For now it's a buildable placeholder so the mode-2 binary exists in the graph.

fn main() -> anyhow::Result<()> {
    eprintln!("codewalk (mode 2) lands at M5 — use `codegraph` for structural mode.");
    Ok(())
}
